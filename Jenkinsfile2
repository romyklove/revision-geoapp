pipeline{
    agent any
    tools{
        maven 'M2_HOME'
    }
    stages{
        stage('maven commands2'){
            steps{
                sh 'mvn clean compile install package'
            }
        }
        stage ('sonarqube scan'){
            steps{
                sh 'mvn sonar:sonar'
            }
        }
    }
}